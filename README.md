# Updating bulk mongodb data using csv file


## Instructions
    
    Open a terminal window and clone the project cd into the project directory
    install node modules by `npm install`
    and finally run `node server.js`
